# The committed package-lock.json must resolve from the public npm registry: a lockfile
# generated behind a corporate proxy bakes that private host into every "resolved" URL, and
# npm honours those URLs over any registry setting — so CI then fails with E401.
registry=https://registry.npmjs.org/
